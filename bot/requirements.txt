# InsureFlow Bot — Dependencies
# Install with: uv add "pipecat-ai[webrtc,deepgram,groq,cartesia,silero]"
# Or with pip:  pip install -r requirements.txt

pipecat-ai[webrtc,deepgram,groq,cartesia,silero]
fastapi
uvicorn[standard]
httpx
mcp[cli]>=1.0.0
python-dotenv
loguru
